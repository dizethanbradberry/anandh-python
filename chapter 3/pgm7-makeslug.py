# Program to write a function make_slug that takes a name converts it into a slug

import re
def make_slug():
	ro=re.findall(r'\w+',st)
	print ('-').join(ro)
st=raw_input("Enter the string:")
make_slug()


