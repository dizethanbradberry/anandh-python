
import sys
import os
def pyfiles(files):
	for f in files:
		if '.py' in f:
			print f,len(open(f).readlines())
	

def main(dirt):		
	files=os.listdir(dirt)
	pyfile=pyfiles(files)


main(sys.argv[1])

	
